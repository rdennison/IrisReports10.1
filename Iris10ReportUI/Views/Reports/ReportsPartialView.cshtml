
@*@using IrisUI.Views.ReportTest*@
@using Telerik.Reporting
@using Telerik.ReportViewer.Mvc

@{ 
    Console.WriteLine("");
}

@*TODO: TE find a way to move this off the partial view*@
<head>
    <style>
        #reportViewArea {
            position: relative;
            width: 890px;
            height: 890px;
            font-family: Verdana, Arial;
        }
    </style>
    <script src="@Url.Content("~/ReportViewer/js/telerikReportViewer-13.0.19.222.min.js")"></script>
</head>

@(Html.TelerikReporting().ReportViewer()
    .Id("reportViewArea")
    .ServiceUrl(Url.Content("/api/reports"))
    //
    // Strongly typed ReportSource - TypeReportSource or UriReportSource.
    .ReportSource((TypeReportSource)Session["ReportTypeSource"])
    .ViewMode(ViewMode.PrintPreview)
    .PageMode(PageMode.SinglePage)
    .ScaleMode(ScaleMode.FitPageWidth)
    .Scale(1.0)
    .PersistSession(false)
    .PrintMode(PrintMode.AutoSelect)
    .ParametersAreaVisible(false)
    
    //.ClientEvents(e => { e.Error("reportError"); e.PageReady("reportPageCount"); })
)


